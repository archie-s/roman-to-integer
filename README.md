All test cases are built into the main program. When the user presses Enter without input, the test suite automatically executes and validates 11 different cases — including valid numerals, invalid inputs, repetitions, and empty values.

## 🧩 Requirements
Before running the program, ensure the following are set up on your system:

| **Requirement**               | **Description**                                                                                                |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **Node.js**                   | Version **v16 or later** (recommended: latest LTS). Used to execute the JavaScript file from the command line. |
| **Operating System**          | Works on **Windows**, **macOS**, or **Linux**.                                                                 |
| **Terminal / Command Prompt** | Required to run Node.js commands (e.g., PowerShell, Terminal, or VS Code Terminal).                            |
| **Text Editor (optional)**    | For viewing or editing the file (e.g., **VS Code**, Sublime Text, or Notepad++).                               |
| **romanToInt.js file**        | The main program file containing the Roman numeral converter and built-in tests.                               |

### ✅ **Quick Setup Steps**

1. Install Node.js from [https://nodejs.org](https://nodejs.org).
2. Download or copy the file **`roman.js`** to your computer.
3. Open your terminal and navigate to the file’s location:

   ```bash
   cd Desktop
   ```
4. Run the program using:

   ```bash
   node roman.js
   ```
